import 'package:flutter/material.dart';

class Drivermaps extends StatelessWidget {
  const Drivermaps({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Maps Page'),
    );
  }
}
